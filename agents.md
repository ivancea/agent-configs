# Agents

This repository is managed with APM and organized as a collection of agent packages.

The general structure is one folder/package per functional feature (for example, `agent-tts` for text-to-speech feedback).

Each module must include a `README.md` with a short summary first, followed by concise workflow/details.
Keep module READMEs updated whenever behavior, workflow, or included skills/agents change.
